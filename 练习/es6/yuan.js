export const mianji = function(r) {
    return 3.14 * r * r;
}

export function zhouchang(r) {
    return 2 * 3.14 * r
}

export var year = 1958;

// 报错（写法通过变量m，还是直接输出 1。1只是一个值，不是接口。）
// var m = 1;
// export m;
